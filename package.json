{
  "name": "qa-dlc-workflows-v2-dev",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "package": "bun scripts/package.ts",
    "package:check": "bun scripts/package.ts --check",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test tests/",
    "check": "bun run typecheck && bun run package:check && bun run test"
  },
  "description": "Dev-only tooling for QA-DLC v2 — not part of the distributable. The shipped framework lives at dist/<harness>/ and is copied into user projects (see v2/README.md). All framework hooks, tools, and sensors run via bun without requiring this package.",
  "license": "MIT",
  "devDependencies": {
    "bun-types": "^1.3.13",
    "typescript": "^5.6.0"
  }
}
